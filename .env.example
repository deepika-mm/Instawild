SESSION_SECRET=your-secret-key-here

# DB and SMTP configuration moved into application code per project settings.
# If you wish to use environment variables instead, restore these entries and update `app.py`.
# MYSQL_HOST=localhost
# MYSQL_USER=root
# MYSQL_PASSWORD=
# MYSQL_DB=animal_detections
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@example.com
# SMTP_PASSWORD=yourpassword
